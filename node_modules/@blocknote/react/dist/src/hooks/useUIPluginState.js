import { useEffect, useState } from "react";
export function useUIPluginState(onUpdate) {
    const [state, setState] = useState();
    useEffect(() => {
        return onUpdate((state) => {
            setState({ ...state });
        });
    }, [onUpdate]);
    return state;
}
//# sourceMappingURL=useUIPluginState.js.map