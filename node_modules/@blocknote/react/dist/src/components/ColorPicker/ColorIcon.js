import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
export const ColorIcon = (props) => {
    const textColor = props.textColor || "default";
    const backgroundColor = props.backgroundColor || "default";
    const size = props.size || 16;
    const style = useMemo(() => ({
        pointerEvents: "none",
        fontSize: (size * 0.75).toString() + "px",
        height: size.toString() + "px",
        lineHeight: size.toString() + "px",
        textAlign: "center",
        width: size.toString() + "px",
    }), [size]);
    return (_jsx("div", { className: "bn-color-icon", "data-background-color": backgroundColor, "data-text-color": textColor, style: style, children: "A" }));
};
//# sourceMappingURL=ColorIcon.js.map