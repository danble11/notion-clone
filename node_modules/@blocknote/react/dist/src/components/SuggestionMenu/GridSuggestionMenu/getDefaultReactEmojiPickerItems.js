import { getDefaultEmojiPickerItems, } from "@blocknote/core";
export async function getDefaultReactEmojiPickerItems(editor, query) {
    return (await getDefaultEmojiPickerItems(editor, query)).map(({ id, onItemClick }) => ({
        id,
        onItemClick,
        icon: id,
    }));
}
//# sourceMappingURL=getDefaultReactEmojiPickerItems.js.map